为了验证 ThemeDist 在 **Vercel** 和 **Netlify** 双端部署的一致性与稳定性，以下为您整理一份完备的项目测试用例规划。

本测试用例分为 **API 接口测试**、**核心业务逻辑测试**、**用户端/管理端页面交互测试**、**双端部署与缓存策略测试** 以及 **优雅降级（容灾）测试** 五大模块。

---

### 测试环境准备
* **Netlify 端点主地址**：`https://themedist.netlify.app`
* **Vercel 端点主地址**：`https://<your-vercel-domain>.vercel.app`（测试时请替换为实际 Vercel 部署域名）

---

### 第一部分：核心 API 接口测试（双端验证）

针对以下所有 API 接口，需分别对 **Vercel** 和 **Netlify** 两端发起请求，比对响应结构的一致性。

#### 1. GET `/api/today.json` 今日主题分发接口
* **测试目的**：验证今日主题分发的数据完整性及双端数据同步性。
* **测试步骤**：
  1. 使用 curl 或 Postman 分别请求双端的 `/api/today.json`。
  2. 验证 HTTP 状态码为 `200`。
  3. 检查返回的 JSON 结构是否包含：`date`, `generatedAt`, `preset`, `presetName`, `dailyIsCommunity`, `cssVars`, `customCss`, `extensions`, `logoText`, `logoColors`, `available`, `directory`。
  4. 检查 `cssVars` 中是否含有完整的 **34个** 自定义属性（包含 8 种 Colors、8 种 Typography、9 种 Spacing、8 种 Effects、2 种 Ambient）。
  5. 检查响应头是否包含 `Access-Control-Allow-Origin: *`（允许 CORS 跨域）。
* **预期结果**：双端返回的数据结构与具体数值完全一致，响应头支持跨域。

#### 2. GET `/api/theme/{preset}.json` 指定主题接口
* **测试目的**：验证预设主题与社区主题静态分发能力。
* **测试步骤**：
  1. 请求一个内置预设端点（例如 `/api/theme/midnight.json`）。
  2. 请求一个已有的社区主题端点（例如 `/api/theme/community-abc12345.json`）。
  3. 检查响应头中的 `Cache-Control`。
* **预期结果**：
  * 内置预设：返回 `Cache-Control` 含有 `public, max-age=31536000, immutable`（365天永久缓存）。
  * 社区主题：返回 `Cache-Control` 包含 `max-age=3600`（1小时缓存），且数据渲染正确。

#### 3. POST `/api/diy/submit.json` 社区主题投稿接口
* **测试目的**：验证投稿的数据校验规则及自动发布特性。
* **测试步骤**：
  * **测试用例 3a (正常提交)**：
    发送 POST 请求，Body 携带合法 JSON：
    ```json
    {
      "name": "Test Neon Theme",
      "author": "Tester",
      "cssVars": { "--color-primary": "#ff00ff", "--color-bg": "#000000" },
      "tags": ["dark", "vibrant"]
    }
    ```
    *预期*：返回 `200` 或 `201 OK`，包含生成的主题 ID（如 `community-xxxxxx`）。
  * **测试用例 3b (缺少必填项)**：
    发送 POST 请求，Body 缺少 `cssVars` 中的 `--color-bg`。
    *预期*：返回 `400 Bad Request`，并提示校验失败。
  * **测试用例 3c (输入安全性过滤)**：
    在 `customCss` 中夹带 XSS 脚本 `<script>alert(1)</script>` 或 CSS 表达式 `expression(...)` 提交。
    *预期*：接口成功接收，但返回的数据中恶意的 HTML 标签或 JavaScript 协议已被净化（通过 `sanitize.ts`）。

#### 4. POST `/api/diy/like.json` 社区主题点赞去重接口
* **测试目的**：验证基于 IP + UA + UUID 三重哈希的点赞防刷机制。
* **测试步骤**：
  1. 使用相同的 IP、User-Agent 和客户端 UUID 连续发送两次点赞 POST 请求。
  2. 验证第一次返回 `{ "likes": N, "voted": true }`（N 为新点赞数）。
  3. 验证第二次返回是否限制重复（如 `voted: false`，点赞数不增加，或返回特定提示）。
  4. 修改请求中的 `User-Agent` 或伪造 `X-Forwarded-For` IP 再次发送。
  5. 验证点赞数成功增加。
* **预期结果**：三重去重逻辑有效，能准确阻止单用户重复刷赞。

---

### 第二部分：核心主题轮换逻辑测试

#### 1. 农历节日匹配测试（基于 Lunar-javascript）
* **测试目的**：验证在传统农历节日当天，系统是否能正确计算并切换为对应的主题及动画效果。
* **测试方法**（可通过修改本地服务器时间，或在测试环境中临时模拟特定日期进行验证）：
  1. 模拟系统日期为农历正月初一（春节）或五月初五（端午节）。
  2. 请求 `/api/today.json`。
  3. 检查返回的 `preset` 是否为对应的节日标识（如 `holiday-l01-01` 或 `holiday-l05-05`）。
  4. 检查 `customCss` 是否非空（包含节日专属特效样式，如灯笼、粒子等）。
  5. 检查 `extensions` 是否包含了对应的 HTML 装饰片段。
* **预期结果**：农历计算无误，成功输出节日主题、特效 CSS 以及 DOM 装饰节点。

#### 2. 公历节日与 Crazy Thursday 轮换测试
* **测试目的**：验证公历特定节日和星期四特殊主题的覆盖优先级。
* **测试方法**：
  1. 模拟系统日期为公历 10 月 31 日（万圣节）。请求 `/api/today.json`，检查 `preset` 是否为 `holiday-10-31`。
  2. 模拟系统日期为任意非节日的**星期四**。请求 `/api/today.json`，检查 `preset` 是否成功切换为 `crazy-thursday`。
* **预期结果**：优先级满足：农历节日 > 公历节日 > 星期四特殊主题 > 社区池轮换 / 日池兜底。

---

### 第三部分：用户端与管理端页面功能测试

需在 Chrome / Safari / Firefox 等浏览器中，分别访问 Vercel 和 Netlify 两端页面进行以下功能路径测试。

#### 1. 主题商店 (`/theme-store`) 交互与缓存
* **测试步骤**：
  1. 打开主题商店页面，观察页面是否成功高亮今日主题。
  2. 尝试使用分类标签（如 `dark`, `light`, `vibrant`）进行过滤。
  3. 验证页面是否即时响应。
  4. 刷新页面，验证列表加载是否命中了 `sessionStorage` 缓存（5 分钟内瞬间渲染，控制台无重复的列表网络请求，仅后台静默刷新）。
* **预期结果**：筛选功能正常，标签自动推断无误，客户端缓存机制正常工作。

#### 2. 主题构建器 (`/theme-builder`) 与 LIVE SENSING 沙箱
* **测试步骤**：
  1. 进入构建器页面，任意修改几个颜色、排版或间距的 CSS 变量。
  2. 观察右侧 16:9 比例的沙箱预览区域是否实时同步更新了样式。
  3. 点击“复制应用代码”按钮，验证剪贴板是否成功获取了对应的 CSS 变量 JSON。
  4. 尝试上传一张本地图片，验证基于 K-means 聚类的图片自动取色功能是否生成了合理的配色方案。
* **预期结果**：沙箱预览零延迟，代码复制及图片取色功能运行正常。

#### 3. 社区投稿页 (`/submit`) 与 AI 生成
* **测试步骤**：
  1. 在投稿表单中填入名称和作者，手动编写或生成一组 CSS 变量。
  2. **AI 规则引擎测试**：在未配置 DeepSeek 密钥的情况下，点击“AI 辅助生成”，输入“深海暗调”，验证是否通过内置规则引擎降级生成了蓝色系的主题。
  3. **DeepSeek 直调测试**：在页面中配置自己的 `sk-` 开头的 DeepSeek API Key，输入相同的描述，验证是否通过客户端直调 DeepSeek 返回了更高质量的结构化主题 JSON。
  4. 提交投稿，验证是否自动重定向或提示发布成功，并在本地生成了带状态（“收录中”）的历史记录。
* **预期结果**：表单校验严密，AI 双级生成逻辑切换自然，提交后在 `/theme-store` 中可立刻找到该主题。

#### 4. 管理端认证与审核 (`/admin`)
* **测试步骤**：
  1. 访问 `/admin` 页面，输入错误的账号密码，验证拦截提示。
  2. 输入正确的 `ADMIN_ACCOUNT` / `ADMIN_PASSWORD` 登录。
  3. 验证是否成功写入了带有 `HttpOnly` 和 `SameSite=Strict` 标记的 `admin_session` 缓存。
  4. 尝试进入待审核列表，对主题进行状态管理操作。
* **预期结果**：权限校验安全可靠，拦截机制符合预期。

---

### 第四部分：双端部署与 CDN 缓存一致性测试

#### 1. Vercel 与 Netlify 双平台发布校验
* **测试目的**：确保通过环境变量 `ADAPTER` 编译出的两套资源在双端表现完全一致。
* **测试步骤**：
  1. 检查 GitHub Actions 部署日志，确保编译 Netlify 版本时注入了 `ADAPTER=netlify`，编译 Vercel 正常运行。
  2. 同时请求 `https://themedist.netlify.app/api/today.json` 与 `https://<your-vercel-domain>.vercel.app/api/today.json`。
  3. 验证两端返回的 `preset`、`cssVars` 数值无偏差。

#### 2. 缓存响应头检查
* **测试步骤**：
  在终端中对双端接口发送带有 `-I` 参数的 curl 请求，检查 `Cache-Control` 返回值。
  ```bash
  curl -I https://themedist.netlify.app/api/today.json
  curl -I https://<your-vercel-domain>.vercel.app/api/today.json
  ```
* **预期结果**：
  * 返回的 Headers 中应包含：
    `Cache-Control: public, max-age=3600, s-maxage=86400, stale-while-revalidate=3600`。
  * 证明浏览器端缓存 1 小时，CDN 边缘缓存 24 小时，且支持 1 小时的静默后台刷新（SWR）。

---

### 第五部分：优雅降级与容灾测试

#### 1. Upstash Redis 数据库不可用（模拟离线）
* **测试目的**：确保在数据库异常时，分发系统不会崩溃，核心服务依然可用。
* **测试方法**：
  1. 在本地开发环境或测试部署中，故意配置错误的 `UPSTASH_REDIS_REST_URL` 或是清空该环境变量。
  2. 访问 `/api/today.json` 接口。
  3. 访问首页 (`/`) 和主题商店 (`/theme-store`)。
* **预期结果**：
  * `/api/today.json` 成功返回，系统由于 Redis 不可用，自动降级去除了社区轮换，转由纯日池或节假日兜底，未抛出 `500` 错误。
  * 页面顶部或控制台显示 `dbAvailable: false`（或提示社区功能降级为只读模式）。
  * 社区投稿和点赞接口返回 `503 Service Unavailable`，并伴随友好的降级提示，而不会导致整站白屏。

#### 2. 客户端无网络请求降级
* **测试目的**：验证客户端集成脚本在网络异常时的鲁棒性。
* **测试方法**：
  1. 打开使用了 ThemeDist 快速开始脚本的测试网页。
  2. 首次加载成功后，控制台应在 `localStorage` 中写入 `td` 数据。
  3. 开启浏览器的 **Offline (离线)** 模式。
  4. 刷新网页。
* **预期结果**：客户端脚本捕获 `fetch` 失败异常，无缝从 `localStorage` 中读取昨日或缓存过的主题，网页视觉效果未发生崩溃或大幅闪烁（无 FOUC）。