# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## About

This repository is currently empty. It was initialized as a sandbox/experimental workspace. As projects are added, document the relevant build, test, and run commands below.

## General conventions (apply when code exists)

- **Build:** `npm run build` or language-specific equivalent
- **Test (all):** `npm test`
- **Test (single):** `npm test -- --grep <pattern>` or framework equivalent
- **Lint:** `npm run lint`

## Project structure (to be filled as code is added)

```
/
├── src/          # Source code
├── tests/        # Test files
├── docs/         # Documentation
└── ...
```
